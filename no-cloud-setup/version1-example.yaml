---
# RPM systems
network:
  version: 1
  config:
    - type: physical
      name: eth0
      subnets:
        - type: dhcp
---
# Debian systems
network:
  version: 1
  config:
    - type: physical
      name: enp1s0
      subnets:
        - type: dhcp